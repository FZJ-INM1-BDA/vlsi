# Extensions to vlsi

This directory collects the extensions to the base `vlsi` specfiication. 

## Background

`vlsi` base specification defines voxel mapping to variable length bytes. Building on top of this base, this directory collects the more specific implementations that transform the bytes to more useful datastrctures.
